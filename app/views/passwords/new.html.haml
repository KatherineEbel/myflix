%section.forgot_password.container
  .row
    .col-sm-10.col-bg-offset-1
      = bootstrap_form_with url: passwords_path, class: 'sign-in' do |f|
        %header
          %h1 Forgot Password?
          %p We will send you an email with a link that you can use to reset your password.
          = f.email_field :email_address
        -#.form-group
        -#  %label Email Address
        -#  .row
        -#    .col-sm-4
        -#      %input.form-control(type="email")
        = f.submit 'Send Email', wrapper: { class: 'action' }
